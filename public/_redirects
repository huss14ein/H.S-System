# API routes hit Netlify functions (keys stay server-side on Netlify).
/api/*  /.netlify/functions/:splat  200
# Stale static deploys on this host → Vercel SPA (auto-deploys from main, zero GitHub secrets).
/*  https://h-s-system.vercel.app/:splat  302!
