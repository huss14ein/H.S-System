# API routes hit Netlify functions first (same-origin AI proxy).
/api/*  /.netlify/functions/:splat  200
# SPA fallback — latest Vite build from dist/ on finova-hussein.netlify.app.
/*  /index.html  200
