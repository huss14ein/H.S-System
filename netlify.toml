[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[functions]
  timeout = 30

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
