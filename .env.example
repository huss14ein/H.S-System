# --- Backend & Database Configuration ---
# Get these from your Supabase project dashboard under Project Settings > API
VITE_SUPABASE_URL="YOUR_SUPABASE_URL"
VITE_SUPABASE_ANON_KEY="YOUR_SUPABASE_ANON_KEY"

# --- AI Configuration (Choose ONE method) ---

# Method 1: Direct Gemini API (Client-side)
# For local development or deployments where you can securely manage this key on the client.
# If this key is present, the app will use it directly, bypassing the Supabase proxy.
# VITE_GEMINI_API_KEY="YOUR_GEMINI_API_KEY"

# Method 2: Supabase Edge Function Proxy (Recommended for Production)
# The client will call a Supabase function which then securely calls Gemini.
# The key must be set as a secret in your Supabase project environment variables.
#
# How to set it up:
# 1. Go to your Supabase project dashboard > Settings > Edge Functions.
# 2. Add a new secret named "GEMINI_API_KEY" with your Google Gemini API key as the value.